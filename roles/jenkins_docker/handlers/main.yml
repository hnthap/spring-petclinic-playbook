---
- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true

- name: Restart jenkins
  ansible.builtin.systemd:
    name: jenkins
    state: restarted
    daemon_reload: true
